# 📦 Kafka Order Processing System (Go + KRaft)

![Go](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)
![Kafka](https://img.shields.io/badge/Apache_Kafka-KRaft-231F20?style=flat&logo=apachekafka)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED?style=flat&logo=docker)

Пет-проект, демонстрирующий работу событийно-ориентированной архитектуры (Event-Driven) на базе **Apache Kafka** в режиме **KRaft** (без ZooKeeper). Реализован сервис обработки заказов с использованием Golang.

## Описание проекта
Приложение имитирует обработку заказов, отправляя данные через Kafka

Есть отправитель (producer) в котором и создается топик с 5 partition и 3 replication
Подключается к кафка через внтуреннюю сеть так как они все запускаются через контейнеры
Но есть файлы .example_env - там вы увидете как подключиться к кафке снаружи
Отправитель отправляет заказы каждые 5 секунд 100 раз, для тесто достаточно
При неудачной отправке мы пробуем отправить еше 5 раз
Ждем ответ в параллельной операции через горутину (анонимная функция)

Есть получатели (consumers) в котором тоже через цикл ждем заказы клиента

Их два

Один работает асинхронно (AUTO_COMMIT=true), записывает заказ - отправляет кафке признак того что 
записал и только после этого берется на след сообщение

Есть второй получатель который копит 10 сообщении (AUTO_COMMIT=false), и только когда их 10 он отправляет кафке признак
что получил сообщения вручную

Есть сервис schema-registy - который следит за типизацией сообщения от producer к consumer
Первым сообщением регистрирует тип и дальше по топику проверяем сообщения

Кафка - работает 3 брокера в 1 кластере
Как мы уже сказали по топику есть 1 топик с 5 partition и 3 replication

Есть сервис для работы с админкой через ui
Используется - provectuslabs/kafka-ui

Есть redis-server для сохранения данных клиента на стороне consumer
И для наблюдения продвижения данных по базе данных
у нас есть еще ui админка redis - redis/redisinsight

## Инструкция по запуску 🚀

1. **Запуск окружения:** запуск очень прост,
    так как все написано в одном docker-compose файле:
    `docker-compose up -d --build`
2. **Запуск Продюсера:**
    если хотим запустить отправителя отдельно то мы можем
    сперва запустить docker-compose файл
    `docker-compose up -d --build`
    перейти в каталог ./proucer/
    и запустить программу go (должен быть установлен go)
    (случае первого запуска `go mod download`)
    `go run .`
    если меняли значения переменных окружения (environment)
    в docker-compose то должны поменять их в своем .env файле
    
3. **Запуск Консьюмера:** ...
    если хотим запустить получателя отдельно то мы можем
    сперва запустить docker-compose файл
    `docker-compose up -d --build`
    перейти в каталог ./consumer/
    и запустить программу go (должен быть установлен go)
    (случае первого запуска `go mod download`)
    `go run .`
    если меняли значения переменных окружения (environment)
    в docker-compose то должны поменять их в своем .env файле


## Как проверить работу приложения ✅

1. Запустите приложения.
2. Перейдите на сайт: http://localhost:8080/ui/clusters/kraft/all-topics
    Вы увидете топик "orders"
    Перейдя по ссылке "orders" вы увидете "messages" как вкладку
    Перейдя по ссылке "messages" вы увидете сообщения по топику "orders" который обновляются каждык 5 секунд
    Это доказывает что мы отправляем сообщения по заказу от отправителя

3. Перейдите на сайт: http://localhost:5540/
    Вы увидете страницу админки redis
    Нажмите чекбокс "I have read and understood the Terms" и нажмите кнопку Submit
    Дальше подключитесь к базе данных через "redis://host.docker.internal:6379" (если вы поменяли внешний порт - надо будет поменять 6379 на него)
    Дальше перейдя по ссылке - вы увидете свою базу в списке и провалившись туда вы увидете список полученных сообщении
    Это доказывает что мы получили данные от отправителя и записываем их с двух получателей
